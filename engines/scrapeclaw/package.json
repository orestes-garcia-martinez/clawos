{
  "name": "@clawos/scrapeclaw-engine",
  "version": "0.4.0",
  "private": true,
  "description": "Deterministic Agent 1 research pipeline for the ClawOS-native ScrapeClaw engine",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --pool=threads"
  },
  "dependencies": {
    "@clawos/shared": "*",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "*",
    "vitest": "^3.2.4"
  }
}

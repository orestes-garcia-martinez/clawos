{
  "name": "@clawos/security",
  "version": "0.1.0",
  "private": true,
  "description": "Zod schemas, rate limiting config, and audit logging for ClawOS",
  "main": "./src/index.ts",
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@clawos/shared": "*",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "*",
    "vitest": "^3.0.0"
  }
}

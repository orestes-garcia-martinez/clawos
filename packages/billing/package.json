{
  "name": "@clawos/billing",
  "version": "0.1.0",
  "private": true,
  "description": "Polar.sh billing client stub",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@clawos/shared": "*"
  },
  "devDependencies": {
    "typescript": "*",
    "vitest": "^3.0.0"
  }
}

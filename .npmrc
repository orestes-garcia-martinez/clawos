# Enforce lockfile — never resolve to latest, always use package-lock.json
package-lock=true
# Fail if lockfile is out of date during npm ci
ci=true
# Engine strict — fail if Node/npm version doesn't match engines field
engine-strict=true
# Disable auto-install of peer deps (explicit control)
legacy-peer-deps=false
